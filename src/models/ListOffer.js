class ListOffer {
  constructor(name, item, content, listingOffer_id, coordinates) {
    (this.name = name),
    (this.item = item),
      (this.content = content),
      (this.listingOffer_id = listingOffer_id);
    this.coordinates = coordinates;
  }
}
export default ListOffer;
